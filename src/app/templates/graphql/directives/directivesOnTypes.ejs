<%for (let index = 0; index < types.length; index++) {
    let schemaDir = types[index].directives_%>
       const <%-types[index].typeName.toLowerCase() %>Directives = {            
         <%- include('../../database/partials/getDirectivesForResolve.ejs', {directive : schemaDir, name : types[index].typeName}) _%>,
         <% for (let j = 0; j < types[index].fields.length; j++) { 
           let field = types[index].fields[j] _%>
         <%- include('../../database/partials/getDirectivesForResolve.ejs', {directive : field.directives, name : field.name}) _%>,
     <%}_%>}
 <%}_%>

 
 module.exports = {<%for (let index = 0; index < types.length; index++) {_%>
    <%-types[index].typeName.toLowerCase() %>Directives,
<%}_%>
}